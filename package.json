{
  "private": true,
  "name": "{{ project-name }}",
  "version": "1.0.0",
  "description": "Scala example of using Cloudflare Workers KV",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{ authors }}",
  "license": "MIT",
  "devDependencies": {
  }
}
